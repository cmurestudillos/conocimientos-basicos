let miArray = []; // Normal
let miArray = new Array(); //Por instancia/clase

let miArray = [1, 2, 3, 4]; // Array de números
let miArray = ["Hola", "que", "tal"]; // Array de Strings
let miArray = [ {propiedad: "valor1" }, { propiedad: "valor2" }]; // Array de objetos
let miArray = [[2, 4], [3, 6]]; // Array de arrays, (Matriz);
let miArray = [1, true, [3,2], "Hola", {clave: "valor"}]; // Array mixto

//Accesos a los elementos del array
let miArray = ["uno", "dos", "tres"];
miArray[1]; // Devuelve: "dos"
miArray.length; // Devuelve 3
miArray[8]; // undefined
