## Lista de Tareas en Angular

> Ejemplo de Aplicacion en Angular en la que se puede añadir y eliminar tareas.

# Pasos para la creacion de la app en Angular
1. npm install -g @angular/cli
2. cd "angular-app"
4. ng serve -o
5. ng build
6. ng test
